자문이 온 뒤 부모가 채운다. 미리 쓰지 마라. 자문은 정답이 아니다.

| 항목 | accept / reject / defer | 이유 |
|------|-------------------------|------|
| | | |

코드로 됨:

코드로 안 됨 (상한):

보고: min(코드점수, 도달상한) = __ · 인용:
host: __  registry: __  slug: __
spawn_ok: __  read_only: __  fallback_used: __
